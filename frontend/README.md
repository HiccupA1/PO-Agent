# PO Agent Frontend

Minimal Next.js interface for running the mock Product Owner Agent.

## Run Locally

```powershell
npm install
npm run dev
```

Set `NEXT_PUBLIC_API_BASE_URL` if the backend is not running on `http://localhost:8000`.
